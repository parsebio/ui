import React from 'react';

const ImmuneDatabase = (props) => {
  const { onDetailsChanged } = props;

  return (
    <div style={{
      display: 'flex', flexDirection: 'column', height: '17vh',
    }}
    />
  );
};

export default ImmuneDatabase;
