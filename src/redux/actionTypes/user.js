const USER = 'user';

/**
 * User was loaded
 */
const USER_LOADED = `${USER}/loaded`;

/**
 * User was not authenticated
 */
const USER_NOT_AUTHENTICATED = `${USER}/notAuthenticated`;

export {
  // eslint-disable-next-line import/prefer-default-export
  USER_LOADED,
  USER_NOT_AUTHENTICATED,
};
